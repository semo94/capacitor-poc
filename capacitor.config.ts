import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.idemia.capacitor.poc',
  appName: 'Capacitor PoC',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
